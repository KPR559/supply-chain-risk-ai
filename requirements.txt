# Backend dependencies live in backend/requirements.txt.
# This shim keeps `pip install -r requirements.txt` working from the repo
# root and lets Vercel resolve the runtime requirements.
-r backend/requirements.txt
